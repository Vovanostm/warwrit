# WP-02.1 — Company domain foundation

- Issue: #9; parent work package: #8
- Base: `d6d67a4c07b3062e282c1e1480769a8e7a1f899f`
- Branch: `feat/wp-02-domain-foundation`
- Authority: Airtable `rec5bphVYSZUTqavX`, approval `rechIKj0hsvfXIvFU`, v1.2 delta `recuq6OOuKnmc1yJL`
- Permission: implementation/tests/PR; no merge, deployment, auto-merge or purchases

## Scope

Dependency-free serializable company types, exact integer money/time, immutable finite definition catalogue, forty-three discriminated command payloads and strict input/actor/source guards. Protocol contains transport-only DTOs and does not import domain behavior. Existing M0 rules and replay are untouched.

Success from `parseCompanyCommand` means shape, finite references and listed context-free predicates only. `guardCompanyCommand` additionally verifies the supplied authenticated adapter context. Neither executes gameplay. `executeCompanyCommand` explicitly rejects even a valid request with `UNSUPPORTED_ACTION` and returns the identical input state until handlers are introduced in later slices.

There are no endpoints, database migrations, payroll, recruitment lifecycle, succession reducer, world simulation, UI or V2 combat implementation in this slice.

## G0 reconstruction

The foundation edition is independently reconstructed from the live Notes named in `WP-02.1-SOURCES.json`; it is not the bytes of either earlier ZIP. Catalogue version `s02-foundation-catalogue-2` and command schema `urn:warwrit:company-command:foundation-2` identify this representation. `s02-domain-provisional-0.2` identifies the source policy profile, not a claim that all its reducers exist.

The catalogue contains the eight enabled skills plus a disabled fixture, 32 perks, six origins, fifteen items, three works and four conditions supported by the read Notes. External culture, location, appearance-service and provider catalogues are not invented. Those IDs require trusted context and later handlers. Cosmetic patch keys and kernel event data remain bounded raw data, not falsely typed executable changes. All handlers fail closed.

Optional fields must be omitted rather than supplied as null in this edition. JSON numbers are finite and integers are safe; exact money and ticks use canonical decimal strings. Resource limits are technical boundaries, not gameplay caps. Date-of-birth offsets may be negative; duration/current campaign ticks and money may not.

Missing archive concordance remains `RC-GAP-MACHINE-01`. This slice does not claim all 117 expected full-domain scenarios are mapped or executed. Add actual source-to-test mappings rather than manufacturing case IDs.

## Trust and determinism

An actor string in a payload is not authorization. A trusted adapter must bind account/company/world and principal. Every non-player request needs an adapter-verified grant bound to command ID, source ID and full canonical request content. PLAYER expectedRevision refers only to the observed public projection. Canonical revision stays in trusted context. Fresh-revision checks are separate so durable retry lookup can precede CAS in WP-02.7.

The JSON schema exported from the same input registry describes transport shape, not authorization or all cross-record predicates. Parser output is a detached frozen copy; exported catalogue and schema registries are immutable. Bounded JSON rejects non-data values and accessors. The core performs no I/O or ambient time/random reads.

## Acceptance

The owner's 2026-09-07 test policy in AGENTS.md supersedes per-command fixture and catalogue-count/hash expectations. `packages/testkit/src/company.spec.test.ts` specifies durable principles: exact conservation, detached value-preserving serialization, atomic rejection, authority and isolation, private/public revision separation, schema/runtime agreement, immutable validation metadata, and causal command admission. It uses independent standards validation and property examples rather than copying the command catalogue. Catalogue content remains versioned production data, not a duplicated test snapshot.

## Review corrections and semantic ownership

Foundation-2 corrects the unreleased foundation-1 wire contract. Unicode lengths count code points; numeric strings have a strict whole-string encoding. Object/array limits agree with the schema. Parsing snapshots data once without caller methods and rejects non-data array prototypes. One freeze helper protects owned registries. Every command declares its actor policy beside its payload shape; there is no implicit PLAYER fallback. The raw protocol DTO deliberately carries an unvalidated version number, while domain parsing accepts only the current version. No production save or V1 combat replay is migrated by this unreleased correction.

| Principle                                              | Foundation admission                                     | Later effect owner                                                    |
| ------------------------------------------------------ | -------------------------------------------------------- | --------------------------------------------------------------------- |
| A critical condition has an explicit deadline          | Require deadline for critical definition                 | WP-02.4 verifies source/time and applies care or death causally       |
| A requested outcome cannot apply before notBefore      | Reject premature ApplyContainerLifecycle/ResolveMissing  | WP-02.4 performs atomic custody/state changes; scheduling is separate |
| A timed task is not a discrete practice event          | Require time-based method compatible with goal           | WP-02.5 checks real access/budget and exact progress                  |
| DEFAULT cannot select a private claim subset           | Require empty client claimIds; TARGETED remains explicit | WP-02.3 resolves all locally due claims and conserves money           |
| Retry does not repeat an effect or expose hidden state | Authentication and fresh revision remain separate        | WP-02.7 does actual durable lookup/transaction/CAS                    |

These checks do not implement future effects. Valid requests still return UNSUPPORTED_ACTION from the foundation dispatcher. AGENTS.md is now synchronized through the owner's explicit request; the original archive-concordance gap is unchanged.

Run actual focused tests, typecheck and repository `pnpm verify`, `pnpm test:combat:stress`, `pnpm test:migrations`. Record environment, commit and results. No static reference check or plan substitutes for those commands; no performance/player evidence is claimed by this headless slice.

## Next slice

WP-02.2 implements opening, immutable identity, membership, physical party and real-graph succession against this foundation. Review definition-field completeness before adding those effects. Resolve source details from canonical records, not historical interview defaults. Preserve the distinction between implementation on a branch and delivery on main.
