# WP-02.3 — Exact local economy and maintenance

Status: implemented in PR #14; issue #13, parent #8. Live delivery status is in CURRENT_PLAN.md and GitHub.
Base: `0ef8e894f7a753cf42864ac97254a2926f3ebd52`.
Canonical contract: `WP02.3-CONTRACT-v1`, Airtable `recveXOcVDyTz6dAZ`,
base `apph3bj1NyVrfJeLM`, Artifacts `tblwAxG5Ek1FyWpiW`.
Read full Notes and Purpose, not historical readiness text alone.

## Authority and source map

| Source | Records / implementation                                   | Obligations                                               |
| ------ | ---------------------------------------------------------- | --------------------------------------------------------- |
| S1     | Approved P1 `reclesS8naNqECbkr` (Risks/Questions)          | Earned claims, local pay, F1, privacy                     |
| S2     | `recE3VjOwNdiFcale`                                        | Exact accrual, deadlines, current support                 |
| S3     | `recuq6OOuKnmc1yJL`                                        | C02-C05/C08; C04 supersedes per-request largest remainder |
| S4     | `reci8qK8KmIhPzasI` + S3 corrections                       | Provisional parameter values, not tested balance          |
| S5     | `rec3eMARPK5nj4pMD`, actual `commands.ts`                  | One command/actor registry                                |
| S6     | `rec39pw7h0ycTk4r7`, `rechtm2nSGSBsTojU`, actual lifecycle | Real requirements, subject keys, retained-data ownership  |
| S7     | `rechIKj0hsvfXIvFU`, `recQNKqYfwoJpCXVu`                   | Accepted physical-group/lore/calendar boundaries          |
| S8     | `rec5bphVYSZUTqavX`, AGENTS, CURRENT_PLAN                  | One writer, staged delivery and postulate-based tests     |

## A — API compatibility, not a new product decision

E1: consume actual OPENING_ASSETS, RECRUIT_SETTLEMENT, DUTY_SETTLEMENT and
LEADERSHIP_SETTLEMENT from the existing lifecycle preparer. Missing recipient,
wage terms and local source bindings require finite trusted adapter evidence.
Return a single lifecycle+finance draft. No supplied `settled=true` can complete it.

E2: Wallet owns cash; a funding pool only identifies the actual local purse and
access scope. Reservations reduce spendable cash, never create a second balance.

E3: existing command schema 2 already rejects a DEFAULT subset/payee and requires
a TARGETED payee. Preserve that registry. New execution rejects zero amounts and
overpayment rather than creating advances, gifts or no-op success.

E4: separate actual earned claims from last-known estimates and backed pending
allocations. Unfunded estimates are not money. Hidden fate cannot release a
reservation or redirect colleagues' visible payments.

## Sequence and boundaries

A sources/pointers; B exact accrual/local cash/C04; C lifecycle financial requirements,
prospective rates, fixed warnings and final/farewell settlement; D FIELD_CAMP/F1;
E finance knowledge/replay; F author review and exact-tree validation.

The pure component is not a SQL transaction, endpoint or full accepted game command.
Real items, care and physical separation remain WP-02.4; practice/social producers
remain WP-02.5; durable atomic composition and SQL races remain WP-02.7.
No generic effect framework, new runtime dependency or global treasury.

## Acceptance groups

P1 exact conservation/partition; P2 split-invariant C04; P3 real rights/location;
P4 earned debt/fixed deadlines; P5 obligations before optional capped farewell;
P6 current agreed physical cohort; P7 paired-world payment/retry chains;
P8 atomic rejection, scoped idempotency and honest PREPARED boundary.

Focused specifications, typecheck and format during work. One final clean code gate:
`pnpm verify`, `pnpm test:combat:stress`, `pnpm test:migrations`.
Evidence from PR #12 is not a result for this slice. PostgreSQL smoke is not
transactional-economy proof. Original source ZIP concordance remains NOT_RUN.

No merge, auto-merge, deploy or paid operations. Full WP-02 and M1 remain incomplete.

## Implemented public boundary

Import `createCompanyEconomyState`, `prepareCompanyEconomy`,
`projectCompanyEconomy`, `projectEconomyRejection` and `quoteCompanyFarewell`
from `@warwrit/game-core`. There is no storage, transport, clock, random source
or runtime dependency in this component.

```ts
const result = prepareCompanyEconomy(state, untrustedCommand, trustedContext);
if (result.kind === 'REJECTED') {
  // result.state === state; no wallet, membership or source receipt was changed.
} else {
  // result.next is ONE combined draft, never an aggregate acceptance.
  // The future composer must satisfy result.receipt.requirements and commit
  // lifecycle, finance, all other affected components and receipts together.
}
```

`CompanyEconomyState` contains the existing `LifecycleState` plus its finance
component. Only the lifecycle's canonical/public revision pair is authoritative;
there is no independently writable finance revision. A replay returns the original
receipt and the unchanged current state, not an earlier state to overwrite it with.
Calling the lifecycle preparer separately and committing only its proposed state
is invalid. An already-applied child lifecycle receipt without the corresponding
combined financial receipt is rejected as inconsistent state.

### Ownership and exact quantities

`Wallet.cashQ` is the sole cash balance. A pool references a company-owned wallet;
two pools cannot create two spendable views of the same purse. A backed reservation
references that claim's real local wallet and reduces spendable cash without
reducing cash on hand. A cash transfer has equal debit and credit. The only explicit
issuance is the source-verified canonical opening endowment, whose amount must
match the real lifecycle opening assets. A recorded opening story debt is an
obligation to its existing provider, not additional starting cash.

Accrual uses `BigInt` internally and canonical exact decimal strings in retained
JSON. Half-open `[fromTick, toTick)` wage intervals yield
`dailyWageMilli * elapsedTicks` q. One campaign day has 1000 ticks; one crown has
1,000,000 q. There is no floating rounding or light-clock conversion. Boundaries
split at service, actual leadership, wage notice, maintenance, day and actual
financial outcome changes. Adjacent identical intervals are coalesced. A technical
catch-up request spanning over 1000 day boundaries is rejected atomically; the
adapter can submit consecutive bounded requests. This is not a product offline cap
or silent truncation.

Food is an interval obligation in person-ticks, not a second inventory. Uncovered
demand yields `FOOD_CONSUMPTION`; the denominator is `COMPANY_RULES.ticksPerDay`.
Field/safe maintenance receipts account only for actual covered intervals and are
not transferable money, stock or experience. A delayed private death can correct
actual intervals and produce the missing uncovered-food requirement; it does not
refund, manufacture or consume physical supplies here. The later composer must
order and satisfy these real resource obligations before accepting the aggregate.

### C04 allocation

For one local pool and due date, retain the original positive claim weights
`a_i`, stable claim IDs, and cumulative commitment `P`. `A(P)` is the prefix of
ordered rational marks `k/a_i`, with ties resolved by claim ID. The next payment
`S` allocates `A(P+S)-A(P)`. Initial counts are `floor(a_i*P/sum(a))`; selecting the
remaining smallest marks takes fewer than the number of claims, using integer
cross-products. Production never enumerates each q.

`DEFAULT` derives all eligible known outstanding claims server-side and proceeds
oldest due date first. `TARGETED` keeps its explicit payee/claim selection and ends
the affected equal-date epochs only. A material eligibility/amount change rebuilds
the affected epoch from then-outstanding amounts. Reload, retries, an unchanged
observation, unrelated maintenance, and conversion of an existing hold into cash
do not reset the unpaid prefix. Zero and overpayment reject without side effects;
neither creates an advance or farewell payment.

### Wage changes, debt and departure

A paid service retains its pre-explained finite tariff and agreed wage. An actual
qualification notice selects that tariff, records the public prospective rate and
applies it at the next campaign-day boundary. It does not change old claims or
produce practice/XP. A notice cannot lower the agreed rate. Original FOUNDER,
FAMILY and PAID membership bases survive changes of effective leadership. The
current or acting effective leader earns no additional self-wage, but earlier
claims survive and the original paid basis resumes when leadership ends.

Arrears are based on known earned obligations, not assumed payment of held money.
An actual communication is required for complaint/final warning. The warning
captures the then-leader/relation and a fixed deadline from the canonical versioned
parameters. Partial pay, a later leader, reloaded state and a farewell gift cannot
move it. Full required payment cures the wage-breach intent only; other causes
remain. Debt survives departure, lack of cash, confirmed captivity/contact loss
and recipient changes after a lawful death report.

Final settlement closes the partial-day claims, pays locally available mandatory
amounts (including existing holds exactly once), and retains unpaid claims. It
returns `PHYSICAL_DEPARTURE`, with the actual intent/container/care-handover
references; it does not set `Membership.endedAt`. Optional farewell requires a
fresh same-tick/revision quote, actual known counterpart, reason-bound intent,
all mandatory obligations to that person paid, significance, real local money and
a cumulative capped grant. `FAREWELL_CONTEXT` binds the existing command to the
actual intent; `quoteCompanyFarewell` is a trusted adapter query, not a player
request that can supply its own eligibility or relationship.

## Finite trusted input contract

`EconomyContext` extends the existing authenticated lifecycle context. Its
`financeFacts` must be resolved by the server from canonical world/service records,
never deserialized from a player body. Every consumed fact is bound to world,
company, canonical revision, source event and the relevant tick. Retained nested
terms, recipients and locations are copied through the shared JSON snapshotter.
A caller-supplied actor kind, receipt ID or `settled=true` is not evidence.

| Fact                   | Required authoritative binding                                                                                                                                 |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OPENING_FUNDS`        | Real opening evidence ID/source, canonical endowment amount and local pool; used only with `OPENING_ASSETS`                                                    |
| `SERVICE_TERMS`        | Actual character/member, local pool, existing recipient, optional actual signing wallet and full agreed qualification tariff; no guessed wage or NPC           |
| `PAYROLL_BINDING`      | Existing membership and actual new local pool when a real duty/arrival/return requirement omits it and the old purse is no longer local; no new wage agreement |
| `LOCAL_MONEY_ACCESS`   | Real active company operator, physical location, allowed local pools and actual recipient wallets; no remote treasury                                          |
| `QUALIFICATION_NOTICE` | Already-informed wage notice from the future actual qualification producer, member, attained lifetime level and unique notice version                          |
| `WAGE_COMMUNICATION`   | Actual message to the counterpart, current effective leader and canonical directed relation at that message                                                    |
| `FAREWELL_CONTEXT`     | Existing member and departure intent/reason, current leader, actual friendship; quote is same-tick and revision-bound                                          |
| `CAMP_SITE`            | Actual stationary party/site, no conflict                                                                                                                      |
| `SAFE_SERVICE_OFFER`   | Existing local capable provider, actual accessible inhabited safe place, live offer/revision/terms and permitted real beneficiary IDs                          |
| `MAINTENANCE_BOUNDARY` | Verified delivered company-local movement/encounter/incompatible-duty/last-field-worker boundary; not a claim that a client clicked logout                     |
| `FINANCIAL_DEATH`      | Actual death tick/cause/custody source and an existing recipient or the subject's unclaimed estate; no physical outcome execution                              |

`AdvanceCampaign.authoritativeInputs` resolves only the finite supported boundary
facts, ordered by their actual ticks. Source replay is checked before rejecting a
previously consumed event for its old tick; conflicting bodies are not silently
accepted. Overall command replay precedence is exact command ID/body, then a
subject-scoped source key, then fresh revision/time admission. A battle source
shared by two characters remains two independent subject effects. Legacy V1 combat
replay and existing command authorization policy are unchanged.

## Maintenance and knowledge boundaries

An accepted F1 agreement names one real co-located group including its effective
leader, at most six people. New intake requires at least one actual local-duty-capable member
and excludes an entirely incapable/unstabilized newcomer group. Already accepted
temporary incapacity is not new intake and does not trigger eviction or healing.
Pre-entry overdue wages must be paid; pre-entry earned but not-yet-due amounts must
be paid or actually backed locally. Old debt never becomes the provider's debt.

A departing companion gets an individual coverage-end boundary; the remaining
accepted group keeps support. A newly arriving or returning person needs an
explicit accepted amendment, without retroactive coverage. A leader leaving the
covered cohort, movement or incompatible activity ends the applicable agreement.
A same-group successor can preserve it. FIELD_CAMP supplies current food only,
continues full wages and stops actual support at loss of the last real basic
worker or a verified activity boundary. F1 has no idle income, extractable surplus,
reserve warehouse, XP or old-debt payment.

Private actual earned periods and actual maintenance boundaries are distinct from
last-known reported liability and coverage estimates. Estimates do not become
claims for unearned money. An unconfirmed allocation is backed but not paid; it
remains unavailable to colleagues and transfers in both hidden-fate worlds. Only
an actual authorized lifecycle observation changes the corresponding financial
knowledge, reconciles excess holds, exposes an existing estate recipient, or
allows actual local settlement. A private lifecycle transition alone does not
publish a maintenance termination. Public projection excludes actual earned
periods, private outcomes/source journals, private relation snapshots and private
canonical revision. Raw PREPARED receipts/requirements are internal, not a network
response or permission to display hidden resource outcomes.

## Versions and residual composition

New, previously undelivered finance serialization: `schemaVersion: 1`, policy
`s02-economy-1`; parameter set `s02-economy-parameters-1` in the existing
`COMPANY_RULES`. Earlier intermediate feature commits are not released save
formats. The common command schema remains 2, common domain ruleset remains
`s02-domain-provisional-0.2`, and existing reconstructed lifecycle catalogue
`s02-lifecycle-catalogue-1` is reused rather than regenerated. No released migration,
V1 combat serialization, lockfile, technology decision or runtime dependency changed.
No old lifecycle-only company receives invented accounts/funds on load: the future
loader must supply its real source-backed service/purse bindings before migration
to this combined draft format.

Unmet finite obligations are `OPENING_NONFINANCIAL`, `RECRUIT_ITEMS`,
`CARE_HANDOVER`, `FOOD_CONSUMPTION`, `PHYSICAL_DEPARTURE`, `OUTCOME_APPLICATION`
and `INFORMED_SOCIAL_CONTRIBUTION`. Actual items, custody, treatment, consumption,
movement/outcome producers and physical member exit are not implemented in this
slice. Social/practice producers remain later work. WP-02.7 must enforce durable
all-or-nothing writes, source receipts, revision races and publication after commit;
a downstream composer cannot accept arbitrary user acknowledgements as fulfillment.
The original ZIP-concordance gap is still NOT_RUN. There is no product-choice delta
or reopening of approved interviews in these engineering bindings.

## Postulate-to-specification evidence map

All names below are public-boundary tests under `packages/testkit/src/`. The four
new specification files have 16 test cases at the review checkpoint; this is an
observed count, not a coverage quota. Properties include many generated cases.
Tests do not pin the complete price catalogue or mirror all command payloads.

| Postulate | Specification/oracle                                                                                                                                                                                   | Focused result |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------- |
| P1        | `company-economy.spec.test.ts`: explicit 30,000 milli × 1,500 ticks; whole vs split intervals/food. `company-economy-lifecycle.spec.test.ts`: prospective notice and real acting-leader transitions    | PASS           |
| P2        | `company-economy.spec.test.ts`: C04 1:3 and 3:5 counterexamples, independent sorted-rational tiny oracle, split/reload, unchanged observation, held-to-cash conversion, TARGETED, oldest due, 10^60 q  | PASS           |
| P3        | Local access/locked area and cash conservation in `company-economy.spec.test.ts`; real opening/recruit/leadership in lifecycle specification; actual duty handover in maintenance specification        | PASS           |
| P4        | Lifecycle specification: fixed final warning through partial payment/reload/deadline and full cure; prospective public notice cannot reprice history                                                   | PASS           |
| P5        | Lifecycle specification: final partial-day debt with empty purse, mandatory-before-farewell/cumulative cap, held and remaining cash settled exactly once, physical exit still required                 | PASS           |
| P6        | `company-economy-maintenance.spec.test.ts`: backed pre-entry, current-only intervals/no cash/XP, old debt refusal, new vs existing incapacity, field interruption, individual departure vs leader exit | PASS           |
| P7        | `company-economy-privacy.spec.test.ts`: paired-world DEFAULT/TARGETED/insufficient/retry chains, identical actual colleague payouts, lawful disclosure, private last-worker camp interruption          | PASS           |
| P8        | Atomic unchanged-state refusal, source/command replay precedence and subject fan-out, invalid actor/extra proof fields, external tariff ownership, no partial membership/ledger commit                 | PASS           |

## Author review and final acceptance record

A separate author review pass checked KISS/DRY/SSOT, actual lifecycle requirements,
value conservation, source boundaries and financial test oracles. This is not an
external review or a second agent's approval. Review corrections include retaining
C04 across no-op observation/hold delivery, scoping resets to the equal-date group,
consuming existing holds before final settlement, preserving individual F1 coverage,
not publishing private camp interruption, binding missing payroll/recipient terms
explicitly, and exposing prospective informed wage notices. The listed regressions
exercise these rules through the public preparer; old foundation/lifecycle behavioral
expectations remain unchanged.

Final acceptance is the existing clean-checkout PR CI on the exact final code tree,
not another local full-gate loop. Record the actual PR head/base/tree, run/job IDs,
three gate command results and logs in issue #13 and the canonical checkpoints.
The source/toolchain export workbench is transport only, excluded from this PR and
not game verification. Do not treat its successful install as a passed gate.

Next step after publication: review the exact WP-02.3 implementation PR and its
clean-gate evidence. No automatic merge or WP-02.4 execution is authorized here.

## 2026-09-08 review correction

The owner requested quality fixes, improved agent guidance, then merge of PR #14
and preparation (not execution) of WP-02.4. This later permission applies to
PR #14 only; earlier no-merge paragraphs describe the initial delivery.

See [the review record](WP-02.3-QUALITY-REVIEW.md) for five reproduced defects,
source-grounded corrections and the focused regression evidence. In particular:
F1 admission considers earned debts across the same person's earlier service;
new service needs `localDuty`, not just field-camp `basicWork`; detached people
are not camp workers; lawful observation and final local settlement preserve
inaccessible old-pool claims; TARGETED closes only due groups it actually changes.

These are corrections to unreleased implementation, not new product decisions
or changes to stored command/schema/catalogue versions. Projection now has a
separate module; the public exports and indivisible PREPARED boundary remain.
